/** @type {import('tailwindcss').Config} */
module.exports = {
  // This tells Tailwind to use the default settings
  // for everything, including the breakpoints (sm, md, lg, etc.)
  theme: {
    extend: {},
  },
}